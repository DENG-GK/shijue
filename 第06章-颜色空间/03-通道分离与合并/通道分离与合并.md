# 通道分离与合并

> 将多通道图像拆分为单通道，或将多个单通道合并为多通道图像，是颜色空间操作的基础技能。掌握了通道操作，你就能对图像的每个颜色分量进行独立处理！

---

## 📖 理论部分

### 1. 什么是通道？

首先回顾一下图像的数据结构：

```
彩色图像的内存布局：

  一张 4×3 的 BGR 彩色图像：
  ┌─────────────────────────────────────────────────┐
  │  img.shape = (3, 4, 3)                          │
  │              │  │  └─ 3个通道（B、G、R）        │
  │              │  └─── 4列（宽度）                │
  │              └───── 3行（高度）                 │
  └─────────────────────────────────────────────────┘

  内存中的样子（简化）：
  ┌─────────────────────────────────────────────────┐
  │  像素(0,0)     像素(0,1)     像素(0,2)     ...   │
  │  [B,G,R]       [B,G,R]       [B,G,R]             │
  │  [32,64,128]   [45,78,156]   [12,34,200]   ...   │
  └─────────────────────────────────────────────────┘

  三个通道：
  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐
  │  B 通道       │ │  G 通道       │ │  R 通道       │
  │  (蓝色分量)   │ │  (绿色分量)   │ │  (红色分量)   │
  │  32  45  12   │ │  64  78  34   │ │ 128 156 200   │
  │  ...          │ │  ...          │ │  ...          │
  └───────────────┘ └───────────────┘ └───────────────┘
```

**通道分离**就是把这三个"层"拆开，变成三张独立的单通道图像。

**通道合并**就是把三张单通道图像"叠"在一起，变回一张多通道图像。

```
通道分离与合并示意：

  彩色图像 (H, W, 3)
  ┌─────────────────┐
  │  █████████████  │
  │  █  彩色图   █  │
  │  █████████████  │
  └─────────────────┘
          │
          │ cv2.split()
          ▼
  ┌─────┐ ┌─────┐ ┌─────┐
  │  B  │ │  G  │ │  R  │    3个单通道图像 (H, W)
  └─────┘ └─────┘ └─────┘
          │
          │ cv2.merge()
          ▼
  ┌─────────────────┐
  │  █████████████  │
  │  █  彩色图   █  │       合并回彩色图像 (H, W, 3)
  │  █████████████  │
  └─────────────────┘
```

---

### 2. 通道分离的三种方法

OpenCV 提供了多种分离通道的方式，各有优劣：

#### 2.1 方法一：cv2.split() 函数

```python
import cv2
import numpy as np

img = cv2.imread("photo.jpg")   # BGR 图像，shape = (H, W, 3)

# 使用 split 分离通道
b, g, r = cv2.split(img)

# 查看结果
print(f"原图: {img.shape}")     # (H, W, 3)
print(f"B通道: {b.shape}")      # (H, W)  ← 变成2D了！
print(f"G通道: {g.shape}")      # (H, W)
print(f"R通道: {r.shape}")      # (H, W)
```

```
cv2.split() 工作原理：

  输入：多通道图像 (H, W, C)
  输出：C 个单通道图像的列表，每个形状为 (H, W)

  彩色 BGR 图像 → 返回 [B, G, R] 三个数组
  HSV 图像     → 返回 [H, S, V] 三个数组
  4通道 BGRA   → 返回 [B, G, R, A] 四个数组
```

#### 2.2 方法二：NumPy 数组索引（推荐！）

```python
import cv2
import numpy as np

img = cv2.imread("photo.jpg")

# 使用数组索引分离通道
b = img[:, :, 0]    # 第一个通道（B）
g = img[:, :, 1]    # 第二个通道（G）
r = img[:, :, 2]    # 第三个通道（R）

# 结果相同
print(f"B通道: {b.shape}")    # (H, W)
```

```
NumPy 索引说明：

  img[:, :, 0] 的含义：
  │   │   └─ 取第0个通道
  │   └───── 取所有列
  └──────── 取所有行

  等价写法：
  img[:, :, 0]   完整写法
  img[..., 0]    使用省略号（推荐）

  性能优势：
  ┌──────────────────────────────────────────────────┐
  │  cv2.split()：创建新的内存副本（慢）             │
  │  img[:,:,0] ：返回视图，不复制数据（快）         │
  │                                                   │
  │  如果只需要读取通道而不修改，索引方式更高效      │
  └──────────────────────────────────────────────────┘
```

⚠️ **重要警告：视图 vs 副本**

```python
import cv2
import numpy as np

img = cv2.imread("photo.jpg")

# 索引方式返回的是"视图"
b_view = img[:, :, 0]
b_view[0, 0] = 255      # 修改视图...
print(img[0, 0, 0])      # 原图也被修改了！→ 255

# 如果需要独立副本，使用 .copy()
b_copy = img[:, :, 0].copy()
b_copy[0, 0] = 0        # 修改副本...
print(img[0, 0, 0])      # 原图不受影响 → 255
```

#### 2.3 方法三：numpy.dsplit()

```python
import cv2
import numpy as np

img = cv2.imread("photo.jpg")

# dsplit = depth split（沿深度方向分割）
channels = np.dsplit(img, 3)    # 分成3份

b = channels[0][:, :, 0]   # 注意：dsplit 保留了第三维
g = channels[1][:, :, 0]
r = channels[2][:, :, 0]

# 或者直接 squeeze 去掉多余维度
b = channels[0].squeeze()   # (H, W, 1) → (H, W)
```

> 💡 这种方法不常用，但在需要沿其他轴分割时（如 hsplit、vsplit）可能会用到。

#### 2.4 三种方法对比

| 方法 | 语法 | 返回类型 | 是否复制 | 性能 | 使用场景 |
|------|------|----------|----------|------|----------|
| `cv2.split()` | `b,g,r = cv2.split(img)` | 副本 | ✅ 复制 | 较慢 | 需要独立修改每个通道 |
| 数组索引 | `b = img[:,:,0]` | 视图 | ❌ 不复制 | 最快 | 只读或临时使用 |
| 数组索引+copy | `b = img[:,:,0].copy()` | 副本 | ✅ 复制 | 较慢 | 需要独立副本 |
| `np.dsplit()` | `np.dsplit(img, 3)` | 保留3D | ✅ 复制 | 较慢 | 特殊需求 |

---

### 3. 通道合并

将多个单通道图像合并成多通道图像：

#### 3.1 cv2.merge() 函数

```python
import cv2
import numpy as np

# 假设有三个单通道图像
b = np.zeros((100, 100), dtype=np.uint8)
g = np.zeros((100, 100), dtype=np.uint8)
r = np.zeros((100, 100), dtype=np.uint8)

# 给每个通道赋予不同的值
b[:] = 255    # 蓝色通道全满
g[:] = 0      # 绿色通道全空
r[:] = 0      # 红色通道全空

# 合并成 BGR 图像
img = cv2.merge([b, g, r])    # 注意：传入列表！

print(f"合并后: {img.shape}")   # (100, 100, 3)
print(f"像素值: {img[0, 0]}")   # [255, 0, 0] → 纯蓝色
```

```
cv2.merge() 的要求：

  ┌─────────────────────────────────────────────────┐
  │  1. 所有输入通道的 shape 必须相同               │
  │     → b.shape == g.shape == r.shape             │
  │                                                  │
  │  2. 所有输入通道的 dtype 必须相同               │
  │     → 都是 uint8 或都是 float32                 │
  │                                                  │
  │  3. 传入的是列表 [b, g, r]，不是元组            │
  │                                                  │
  │  4. 通道顺序决定最终颜色空间                    │
  │     → [b, g, r] 得到 BGR                        │
  │     → [r, g, b] 得到 RGB                        │
  └─────────────────────────────────────────────────┘
```

#### 3.2 NumPy 的 dstack()

```python
import numpy as np

# dstack = depth stack（沿深度方向堆叠）
b = np.zeros((100, 100), dtype=np.uint8)
g = np.zeros((100, 100), dtype=np.uint8)
r = np.zeros((100, 100), dtype=np.uint8)

img = np.dstack([b, g, r])
print(f"shape: {img.shape}")    # (100, 100, 3)
```

#### 3.3 分离与合并的对称性

```python
import cv2
import numpy as np

img = cv2.imread("photo.jpg")

# 分离
b, g, r = cv2.split(img)

# 合并（恢复原图）
img_restored = cv2.merge([b, g, r])

# 验证是否一致
print(f"完全一致: {np.array_equal(img, img_restored)}")   # True
```

---

### 4. 单通道可视化

单通道图像默认显示为灰度图。但有时我们想用彩色方式展示某个通道的分布：

#### 4.1 灰度显示（默认）

```python
import cv2

img = cv2.imread("photo.jpg")
b, g, r = cv2.split(img)

# 直接显示单通道 → 灰度图
cv2.imshow("B Channel (Gray)", b)
cv2.waitKey(0)
```

```
为什么单通道显示为灰度？

  单通道图像 shape = (H, W)，只有一个值表示"强度"
  ┌─────────────────────────────────────────────────┐
  │  像素值 = 0   → 显示为黑色                      │
  │  像素值 = 128 → 显示为灰色                      │
  │  像素值 = 255 → 显示为白色                      │
  │                                                  │
  │  没有颜色信息，只有明暗信息                     │
  └─────────────────────────────────────────────────┘
```

#### 4.2 彩色可视化单通道

如果想用颜色突出显示某个通道，可以将其他通道置零后合并：

```python
import cv2
import numpy as np

img = cv2.imread("photo.jpg")
b, g, r = cv2.split(img)

# 创建零数组
zeros = np.zeros_like(b)

# 只保留蓝色通道（其他通道置零）
blue_only = cv2.merge([b, zeros, zeros])    # 蓝色可视化

# 只保留绿色通道
green_only = cv2.merge([zeros, g, zeros])   # 绿色可视化

# 只保留红色通道
red_only = cv2.merge([zeros, zeros, r])     # 红色可视化

# 显示对比
cv2.imshow("Blue Channel", blue_only)
cv2.imshow("Green Channel", green_only)
cv2.imshow("Red Channel", red_only)
cv2.waitKey(0)
```

```
彩色可视化效果：

  原图中某个像素 BGR = (200, 100, 50)

  B通道值=200：
    blue_only  = [200, 0, 0]   → 深蓝色

  G通道值=100：
    green_only = [0, 100, 0]   → 暗绿色

  R通道值=50：
    red_only   = [0, 0, 50]    → 暗红色

  → 通道值越大，对应颜色越亮
```

#### 4.3 使用伪彩色映射（Colormap）

OpenCV 提供了 `applyColorMap()` 函数，可以将灰度图转换为彩色热力图：

```python
import cv2

img = cv2.imread("photo.jpg")
b, g, r = cv2.split(img)

# 使用 JET 伪彩色映射
b_colormap = cv2.applyColorMap(b, cv2.COLORMAP_JET)

# 使用 HOT 伪彩色映射
b_hot = cv2.applyColorMap(b, cv2.COLORMAP_HOT)

cv2.imshow("B Channel - JET", b_colormap)
cv2.imshow("B Channel - HOT", b_hot)
cv2.waitKey(0)
```

```
常用的 Colormap：

  ┌────────────────┬─────────────────────────────────┐
  │ 名称            │ 效果                            │
  ├────────────────┼─────────────────────────────────┤
  │ COLORMAP_JET   │ 蓝→青→绿→黄→红（彩虹色）        │
  │ COLORMAP_HOT   │ 黑→红→黄→白（热力图）           │
  │ COLORMAP_BONE  │ 蓝灰色调（医学影像）            │
  │ COLORMAP_COOL  │ 青→紫（冷色调）                 │
  │ COLORMAP_VIRIDIS│ 紫→绿→黄（科学可视化）         │
  └────────────────┴─────────────────────────────────┘
```

---

### 5. 通道操作的应用场景

#### 5.1 修改单个通道

```python
import cv2
import numpy as np

img = cv2.imread("photo.jpg")

# 方法1：分离→修改→合并
b, g, r = cv2.split(img)
b = cv2.add(b, 50)   # 增加蓝色分量（饱和加法）
img_blue = cv2.merge([b, g, r])

# 方法2：直接修改（更高效）
img_blue2 = img.copy()
img_blue2[:, :, 0] = cv2.add(img_blue2[:, :, 0], 50)

# 方法3：使用 NumPy（注意溢出！）
img_blue3 = img.copy()
# img_blue3[:, :, 0] += 50   # ❌ 危险！会溢出
# 正确做法：
img_blue3[:, :, 0] = np.clip(img_blue3[:, :, 0].astype(np.int16) + 50, 0, 255).astype(np.uint8)
```

```
整数溢出问题：

  假设某像素 B=230，想加 50：

  NumPy 默认行为（uint8）：
    230 + 50 = 280 → 溢出 → 280 - 256 = 24  ❌ 变暗了！

  cv2.add 的行为（饱和加法）：
    230 + 50 = 280 → 饱和 → 255  ✅ 正确！

  → 进行加减运算时，优先使用 cv2.add / cv2.subtract
```

#### 5.2 交换通道

```python
import cv2

img = cv2.imread("photo.jpg")

# 交换 B 和 R 通道 → BGR 变成 RGB
img_rgb = img[:, :, ::-1]    # 快速翻转

# 或者明确交换
b, g, r = cv2.split(img)
img_rgb = cv2.merge([r, g, b])   # 按 RGB 顺序合并

# 用于 matplotlib 显示
import matplotlib.pyplot as plt
plt.imshow(img_rgb)   # 正确的颜色
plt.show()
```

#### 5.3 在 HSV 空间分离通道

```python
import cv2

img = cv2.imread("photo.jpg")

# 转换到 HSV
hsv = cv2.cvtColor(img, cv2.COLOR_BGR2HSV)

# 分离 H, S, V 通道
h, s, v = cv2.split(hsv)

print(f"色调 H: min={h.min()}, max={h.max()}")      # 0-179
print(f"饱和度 S: min={s.min()}, max={s.max()}")    # 0-255
print(f"明度 V: min={v.min()}, max={v.max()}")      # 0-255

# 可视化
cv2.imshow("Hue", h)
cv2.imshow("Saturation", s)
cv2.imshow("Value", v)
cv2.waitKey(0)
```

#### 5.4 单独调整亮度（在 Lab 空间）

```python
import cv2

img = cv2.imread("photo.jpg")

# 转换到 Lab 空间
lab = cv2.cvtColor(img, cv2.COLOR_BGR2Lab)

# 分离通道
l, a, b = cv2.split(lab)

# 只调整 L 通道（亮度）
l = cv2.add(l, 30)    # 增加亮度

# 合并回去
lab_bright = cv2.merge([l, a, b])

# 转回 BGR
img_bright = cv2.cvtColor(lab_bright, cv2.COLOR_Lab2BGR)

cv2.imshow("Original", img)
cv2.imshow("Brighter", img_bright)
cv2.waitKey(0)
```

#### 5.5 创建 Alpha 通道（透明度）

```python
import cv2
import numpy as np

img = cv2.imread("photo.jpg")  # BGR, 3通道
h, w = img.shape[:2]

# 创建 Alpha 通道（全不透明）
alpha = np.ones((h, w), dtype=np.uint8) * 255

# 合并为 BGRA（4通道）
bgra = cv2.merge([img[:,:,0], img[:,:,1], img[:,:,2], alpha])

# 或者使用 cvtColor
bgra2 = cv2.cvtColor(img, cv2.COLOR_BGR2BGRA)

print(f"BGRA shape: {bgra.shape}")    # (H, W, 4)

# 保存为支持透明度的 PNG
cv2.imwrite("output_with_alpha.png", bgra)
```

```
Alpha 通道说明：

  BGRA 图像有 4 个通道：
  ┌─────────────────────────────────────────────────┐
  │  B - 蓝色分量                                   │
  │  G - 绿色分量                                   │
  │  R - 红色分量                                   │
  │  A - 透明度（0=完全透明，255=完全不透明）       │
  └─────────────────────────────────────────────────┘

  PNG 支持 Alpha 通道，JPEG 不支持！
```

---

### 6. 性能考虑

```python
import cv2
import numpy as np
import time

# 创建大图像
img = np.random.randint(0, 256, (4000, 6000, 3), dtype=np.uint8)

# 测试 cv2.split 性能
start = time.time()
for _ in range(100):
    b, g, r = cv2.split(img)
print(f"cv2.split: {time.time() - start:.3f}s")

# 测试索引性能
start = time.time()
for _ in range(100):
    b = img[:, :, 0]
    g = img[:, :, 1]
    r = img[:, :, 2]
print(f"索引方式: {time.time() - start:.3f}s")

# 测试索引+copy性能
start = time.time()
for _ in range(100):
    b = img[:, :, 0].copy()
    g = img[:, :, 1].copy()
    r = img[:, :, 2].copy()
print(f"索引+copy: {time.time() - start:.3f}s")
```

```
典型性能结果（参考）：

  ┌─────────────────┬─────────────┬───────────────────┐
  │ 方法             │ 时间        │ 说明              │
  ├─────────────────┼─────────────┼───────────────────┤
  │ cv2.split       │ ~1.5s       │ 创建3个副本       │
  │ 索引 [:,:,x]    │ ~0.001s     │ 只返回视图        │
  │ 索引+.copy()    │ ~1.5s       │ 显式创建副本      │
  └─────────────────┴─────────────┴───────────────────┘

  → 如果只需读取，用索引
  → 如果需要修改且不影响原图，用 cv2.split 或 .copy()
```

---

## 💻 代码实战

### 实战1：通道分离与可视化

```python
import cv2
import numpy as np
import os

# 设置路径
IMAGES_DIR = os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "images")
SRC_IMG = os.path.join(os.path.dirname(os.path.abspath(__file__)),
                       "..", "..", "01-颜色空间理论", "images", "color_shapes.png")

# 读取或创建测试图像
if not os.path.exists(SRC_IMG):
    print("创建测试图像...")
    img = np.zeros((400, 600, 3), dtype=np.uint8)
    cv2.circle(img, (150, 200), 80, (255, 0, 0), -1)    # 蓝色圆
    cv2.circle(img, (300, 200), 80, (0, 255, 0), -1)    # 绿色圆
    cv2.circle(img, (450, 200), 80, (0, 0, 255), -1)    # 红色圆
else:
    img = cv2.imread(SRC_IMG)

os.makedirs(IMAGES_DIR, exist_ok=True)

# === 方法1：使用 cv2.split ===
b, g, r = cv2.split(img)
print(f"原图 shape: {img.shape}")
print(f"单通道 shape: {b.shape}")

# === 方法2：使用数组索引 ===
b2 = img[:, :, 0]
g2 = img[:, :, 1]
r2 = img[:, :, 2]
print(f"索引方式结果一致: {np.array_equal(b, b2)}")

# 保存灰度形式的单通道图
cv2.imwrite(os.path.join(IMAGES_DIR, "channel_b_gray.png"), b)
cv2.imwrite(os.path.join(IMAGES_DIR, "channel_g_gray.png"), g)
cv2.imwrite(os.path.join(IMAGES_DIR, "channel_r_gray.png"), r)
print("灰度通道图已保存")
```

### 实战2：彩色通道可视化

```python
import cv2
import numpy as np
import os

IMAGES_DIR = os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "images")

# 读取图像
img = cv2.imread(os.path.join(IMAGES_DIR, "..", "..", "01-颜色空间理论", "images", "color_shapes.png"))
if img is None:
    img = np.zeros((400, 600, 3), dtype=np.uint8)
    cv2.circle(img, (150, 200), 80, (255, 0, 0), -1)
    cv2.circle(img, (300, 200), 80, (0, 255, 0), -1)
    cv2.circle(img, (450, 200), 80, (0, 0, 255), -1)

b, g, r = cv2.split(img)

# 创建零数组
zeros = np.zeros_like(b)

# 彩色可视化
blue_only = cv2.merge([b, zeros, zeros])
green_only = cv2.merge([zeros, g, zeros])
red_only = cv2.merge([zeros, zeros, r])

# 保存
os.makedirs(IMAGES_DIR, exist_ok=True)
cv2.imwrite(os.path.join(IMAGES_DIR, "blue_only.png"), blue_only)
cv2.imwrite(os.path.join(IMAGES_DIR, "green_only.png"), green_only)
cv2.imwrite(os.path.join(IMAGES_DIR, "red_only.png"), red_only)

# 创建对比图
comparison = np.hstack([img, blue_only, green_only, red_only])
cv2.imwrite(os.path.join(IMAGES_DIR, "channel_comparison.png"), comparison)
print(f"对比图已保存: {os.path.join(IMAGES_DIR, 'channel_comparison.png')}")
```

### 实战3：通道合并与验证

```python
import cv2
import numpy as np

# 创建彩色图像
img = np.random.randint(0, 256, (100, 100, 3), dtype=np.uint8)
print(f"原图: {img.shape}, dtype={img.dtype}")

# 分离
b, g, r = cv2.split(img)
print(f"分离后: B={b.shape}, G={g.shape}, R={r.shape}")

# 合并
merged = cv2.merge([b, g, r])
print(f"合并后: {merged.shape}")

# 验证一致性
print(f"与原图一致: {np.array_equal(img, merged)}")

# 交换通道实验
swapped = cv2.merge([r, g, b])   # R 和 B 交换位置
print(f"交换后与原图一致: {np.array_equal(img, swapped)}")  # False
```

### 实战4：HSV 通道分析

```python
import cv2
import numpy as np

# 创建包含多种颜色的测试图
img = np.zeros((300, 400, 3), dtype=np.uint8)
cv2.rectangle(img, (0, 0), (100, 300), (0, 0, 255), -1)     # 红色
cv2.rectangle(img, (100, 0), (200, 300), (0, 255, 0), -1)   # 绿色
cv2.rectangle(img, (200, 0), (300, 300), (255, 0, 0), -1)   # 蓝色
cv2.rectangle(img, (300, 0), (400, 300), (0, 255, 255), -1) # 黄色

# 转换到 HSV
hsv = cv2.cvtColor(img, cv2.COLOR_BGR2HSV)

# 分离 HSV 通道
h, s, v = cv2.split(hsv)

print("各颜色区域的 H 值（OpenCV 范围 0-179）：")
print(f"  红色区域 H: {h[150, 50]}")      # 约 0 或接近 179
print(f"  绿色区域 H: {h[150, 150]}")     # 约 60
print(f"  蓝色区域 H: {h[150, 250]}")     # 约 120
print(f"  黄色区域 H: {h[150, 350]}")     # 约 30

# 可视化 H 通道（使用伪彩色）
h_color = cv2.applyColorMap(h, cv2.COLORMAP_HSV)
cv2.imwrite("h_channel_colormap.png", h_color)
```

### 实战5：创建透明 PNG

```python
import cv2
import numpy as np

# 创建一个带渐变透明度的图像
size = 400
img = np.zeros((size, size, 3), dtype=np.uint8)
cv2.circle(img, (size//2, size//2), 150, (0, 165, 255), -1)  # 橙色圆

# 创建渐变 Alpha 通道（中心不透明，边缘透明）
alpha = np.zeros((size, size), dtype=np.uint8)
cv2.circle(alpha, (size//2, size//2), 150, 255, -1)

# 高斯模糊使边缘柔和
alpha = cv2.GaussianBlur(alpha, (21, 21), 0)

# 合并为 BGRA
b, g, r = cv2.split(img)
bgra = cv2.merge([b, g, r, alpha])

# 保存为 PNG（支持透明度）
cv2.imwrite("gradient_alpha.png", bgra)
print(f"透明 PNG 已保存，shape: {bgra.shape}")
```

---

## 🧠 小测验

1. `cv2.split()` 和数组索引 `img[:,:,0]` 的主要区别是什么？（答：split 返回副本，索引返回视图）
2. 如何只保留蓝色通道并显示为蓝色？（答：`cv2.merge([b, zeros, zeros])`）
3. `cv2.merge()` 的参数应该是什么类型？（答：列表，如 `[b, g, r]`）
4. 修改索引得到的通道 `b = img[:,:,0]; b[0,0] = 255`，原图会变吗？（答：会，因为是视图）
5. 如何创建 4 通道 BGRA 图像？（答：`cv2.merge([b, g, r, alpha])` 或 `cv2.cvtColor(img, cv2.COLOR_BGR2BGRA)`）

---

## 📌 本节要点

```
✅ 通道分离：cv2.split() 返回副本，索引方式返回视图
✅ 通道合并：cv2.merge([channel_list])，传入列表
✅ 视图 vs 副本：修改视图会影响原图，需要独立操作时用 .copy()
✅ 彩色可视化：将其他通道置零，只保留目标通道
✅ 伪彩色映射：cv2.applyColorMap() 可将灰度转为热力图
✅ 性能优化：只读时用索引，需要修改时用 split 或 copy
✅ Alpha 通道：BGRA 格式支持透明度，保存为 PNG
```

---

**下一步：** 👉 [04 - 颜色空间应用](../04-颜色空间应用/颜色空间应用.md)
