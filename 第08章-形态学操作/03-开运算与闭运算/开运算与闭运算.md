# 开运算与闭运算

> 开运算（Opening）和闭运算（Closing）是腐蚀和膨胀的组合操作。开运算 = 先腐蚀后膨胀，专门用来去除噪点；闭运算 = 先膨胀后腐蚀，专门用来填补空洞。它们是图像预处理中最常用的形态学操作！

---

## 📖 理论部分

### 1. 开运算（Opening）

#### 1.1 开运算的定义

开运算 = **先腐蚀，后膨胀**（使用相同的结构元素）

```
开运算的过程：

  原始图像        腐蚀           膨胀          最终结果
  ┌────────┐    ┌────────┐    ┌────────┐    ┌────────┐
  │ ▓▓▓▓▓  │    │  ▓▓▓   │    │ ▓▓▓▓▓  │    │ ▓▓▓▓▓  │
  │ ▓▓▓▓▓  │ →  │  ▓▓▓   │ →  │ ▓▓▓▓▓  │  = │ ▓▓▓▓▓  │
  │ ▓▓▓▓▓  │    │  ▓▓▓   │    │ ▓▓▓▓▓  │    │ ▓▓▓▓▓  │
  │   ·    │    │        │    │        │    │        │
  │  · ·   │    │        │    │        │    │        │
  └────────┘    └────────┘    └────────┘    └────────┘
     小噪点    → 噪点消失  → 主体恢复   → 噪点没了！
               主体变小     主体变回来

  数学表示：Opening(A, B) = Dilate(Erode(A, B), B)
```

#### 1.2 开运算的效果

```
开运算的主要效果：

  ┌─────────────────────────────────────────────────────────────┐
  │  1. 去除小噪点                                              │
  │     小于结构元素的白色区域会被消除                          │
  │                                                             │
  │  2. 平滑物体边缘                                            │
  │     去除边缘的小突起                                        │
  │                                                             │
  │  3. 断开细连接                                              │
  │     细于结构元素的连接会被切断                              │
  │                                                             │
  │  4. 保持物体大小基本不变                                    │
  │     因为腐蚀后又膨胀回来了                                  │
  └─────────────────────────────────────────────────────────────┘

  形象比喻：
  就像用砂纸打磨木头表面：
  • 小毛刺被磨掉了（噪点消除）
  • 但木头的整体大小没变
```

#### 1.3 开运算去噪原理

```
为什么开运算能去除噪点？

  原理图解：

  原始状态             腐蚀后               膨胀后
  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
  │              │   │              │   │              │
  │  ████████    │   │   ██████     │   │  ████████    │ ← 主物体恢复
  │  ████████    │   │   ██████     │   │  ████████    │
  │  ████████    │   │   ██████     │   │  ████████    │
  │              │   │              │   │              │
  │     ██       │   │              │   │              │ ← 噪点没了！
  │    ██        │   │              │   │              │
  └──────────────┘   └──────────────┘   └──────────────┘

  解释：
  • 腐蚀时，小噪点完全被"蚀掉"，变成了背景
  • 主物体虽然变小，但还存在
  • 膨胀时，主物体恢复，但噪点已经消失，无法恢复

  关键条件：
  噪点的尺寸 < 结构元素的尺寸
```

---

### 2. 闭运算（Closing）

#### 2.1 闭运算的定义

闭运算 = **先膨胀，后腐蚀**（使用相同的结构元素）

```
闭运算的过程：

  原始图像        膨胀           腐蚀          最终结果
  ┌────────┐    ┌────────┐    ┌────────┐    ┌────────┐
  │ ▓▓▓▓▓  │    │▓▓▓▓▓▓▓ │    │ ▓▓▓▓▓  │    │ ▓▓▓▓▓  │
  │ ▓▓ ▓▓  │ →  │▓▓▓▓▓▓▓ │ →  │ ▓▓▓▓▓  │  = │ ▓▓▓▓▓  │
  │ ▓▓▓▓▓  │    │▓▓▓▓▓▓▓ │    │ ▓▓▓▓▓  │    │ ▓▓▓▓▓  │
  └────────┘    └────────┘    └────────┘    └────────┘
     有空洞    → 空洞填上  → 恢复大小   → 空洞没了！
               物体变大     物体变回来

  数学表示：Closing(A, B) = Erode(Dilate(A, B), B)
```

#### 2.2 闭运算的效果

```
闭运算的主要效果：

  ┌─────────────────────────────────────────────────────────────┐
  │  1. 填补小空洞                                              │
  │     小于结构元素的黑色区域（洞）会被填补                    │
  │                                                             │
  │  2. 连接邻近物体                                            │
  │     间隙小于结构元素的物体会连接起来                        │
  │                                                             │
  │  3. 平滑边缘凹陷                                            │
  │     填补边缘的小凹口                                        │
  │                                                             │
  │  4. 保持物体大小基本不变                                    │
  │     因为膨胀后又腐蚀回来了                                  │
  └─────────────────────────────────────────────────────────────┘

  形象比喻：
  就像用腻子填补墙上的小裂缝和洞：
  • 小洞被填上了
  • 但墙的整体厚度没变
```

#### 2.3 闭运算填洞原理

```
为什么闭运算能填补空洞？

  原理图解：

  原始状态             膨胀后               腐蚀后
  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
  │  ████████    │   │ ██████████   │   │  ████████    │
  │  ██    ██    │   │ ██████████   │   │  ████████    │ ← 洞被填上了
  │  ██    ██    │   │ ██████████   │   │  ████████    │
  │  ████████    │   │ ██████████   │   │  ████████    │
  └──────────────┘   └──────────────┘   └──────────────┘

  解释：
  • 膨胀时，边缘向内扩张，小空洞被填上
  • 物体整体变大
  • 腐蚀时，外边缘收缩回原来大小
  • 但内部的洞已经被填上，不会再出现

  关键条件：
  空洞的尺寸 < 结构元素的尺寸
```

---

### 3. 开运算 vs 闭运算 对比

```
开运算与闭运算的对比：

  ┌───────────────┬─────────────────────┬─────────────────────┐
  │     特性      │       开运算        │       闭运算        │
  ├───────────────┼─────────────────────┼─────────────────────┤
  │   操作顺序    │   先腐蚀后膨胀      │   先膨胀后腐蚀      │
  ├───────────────┼─────────────────────┼─────────────────────┤
  │   主要功能    │   去除白色噪点      │   填补黑色空洞      │
  ├───────────────┼─────────────────────┼─────────────────────┤
  │   边缘效果    │   去除小突起        │   填补小凹陷        │
  ├───────────────┼─────────────────────┼─────────────────────┤
  │   连接性      │   可能断开细连接    │   可能连接邻近物体  │
  ├───────────────┼─────────────────────┼─────────────────────┤
  │   物体大小    │   基本保持不变      │   基本保持不变      │
  ├───────────────┼─────────────────────┼─────────────────────┤
  │   适用场景    │   去噪、分离物体    │   填洞、连接断裂    │
  └───────────────┴─────────────────────┴─────────────────────┘

  记忆技巧：
  • 开运算（Opening）：像开门一样，把噪点"赶出去"
  • 闭运算（Closing）：像关门一样，把洞"关上"

  或者：
  • 开运算：去"白"（白噪点）
  • 闭运算：填"黑"（黑空洞）
```

---

### 4. 组合使用

开运算和闭运算经常组合使用，以同时去除噪点和填补空洞：

```
组合使用策略：

  ① 先开后闭（更常用）
  ┌────────┐    ┌────────┐    ┌────────┐
  │ 带噪点  │ →  │ 噪点没  │ →  │ 洞填上  │
  │ 有空洞  │    │ 有空洞  │    │ 干净了  │
  └────────┘    └────────┘    └────────┘
     原图       开运算后      闭运算后

  ② 先闭后开
  ┌────────┐    ┌────────┐    ┌────────┐
  │ 带噪点  │ →  │ 洞填上  │ →  │ 噪点没  │
  │ 有空洞  │    │ 有噪点  │    │ 干净了  │
  └────────┘    └────────┘    └────────┘
     原图       闭运算后      开运算后

  选择建议：
  • 噪点多于空洞：先开后闭
  • 空洞多于噪点：先闭后开
  • 一般情况：先开后闭效果更好
```

---

## 💻 代码实战

### 代码1：开运算基础

```python
"""
开运算基础
演示 cv2.morphologyEx() 配合 cv2.MORPH_OPEN 的使用
"""

import cv2
import numpy as np
import matplotlib.pyplot as plt

plt.rcParams['font.sans-serif'] = ['SimHei', 'Microsoft YaHei']
plt.rcParams['axes.unicode_minus'] = False

# ===================== 创建带噪点的图像 =====================

def create_noisy_image():
    """创建一个带有白色噪点的二值图像"""
    img = np.zeros((200, 300), dtype=np.uint8)

    # 主要物体
    cv2.rectangle(img, (30, 30), (130, 130), 255, -1)
    cv2.circle(img, (200, 80), 50, 255, -1)

    # 添加白色噪点（随机小点）
    np.random.seed(42)
    for _ in range(80):
        x = np.random.randint(0, 300)
        y = np.random.randint(0, 200)
        r = np.random.randint(2, 5)
        cv2.circle(img, (x, y), r, 255, -1)

    return img

# ===================== 开运算 =====================

noisy = create_noisy_image()
kernel = cv2.getStructuringElement(cv2.MORPH_RECT, (7, 7))

# 方法1：使用 morphologyEx
opened = cv2.morphologyEx(noisy, cv2.MORPH_OPEN, kernel)

# 方法2：等价于先腐蚀后膨胀（验证用）
eroded = cv2.erode(noisy, kernel)
opened_manual = cv2.dilate(eroded, kernel)

# ===================== 可视化 =====================

fig, axes = plt.subplots(2, 2, figsize=(12, 10))

axes[0, 0].imshow(noisy, cmap='gray')
axes[0, 0].set_title('原图（带噪点）', fontsize=12)
axes[0, 0].axis('off')

axes[0, 1].imshow(opened, cmap='gray')
axes[0, 1].set_title('开运算结果', fontsize=12)
axes[0, 1].axis('off')

axes[1, 0].imshow(eroded, cmap='gray')
axes[1, 0].set_title('中间步骤：腐蚀', fontsize=12)
axes[1, 0].axis('off')

axes[1, 1].imshow(opened_manual, cmap='gray')
axes[1, 1].set_title('腐蚀后膨胀（= 开运算）', fontsize=12)
axes[1, 1].axis('off')

plt.suptitle('开运算去噪效果', fontsize=14, fontweight='bold')
plt.tight_layout()
plt.savefig('opening_basic.png', dpi=150)
plt.show()

# 验证两种方法结果一致
print("两种方法结果是否一致:", np.array_equal(opened, opened_manual))
```

---

### 代码2：闭运算基础

```python
"""
闭运算基础
演示 cv2.morphologyEx() 配合 cv2.MORPH_CLOSE 的使用
"""

import cv2
import numpy as np
import matplotlib.pyplot as plt

plt.rcParams['font.sans-serif'] = ['SimHei', 'Microsoft YaHei']
plt.rcParams['axes.unicode_minus'] = False

# ===================== 创建带空洞的图像 =====================

def create_holed_image():
    """创建一个带有空洞的二值图像"""
    img = np.zeros((200, 300), dtype=np.uint8)

    # 主要物体
    cv2.rectangle(img, (30, 30), (130, 170), 255, -1)
    cv2.circle(img, (200, 100), 60, 255, -1)

    # 添加黑色空洞
    cv2.circle(img, (80, 80), 15, 0, -1)
    cv2.circle(img, (80, 130), 10, 0, -1)
    cv2.rectangle(img, (50, 100), (65, 115), 0, -1)

    cv2.circle(img, (200, 100), 20, 0, -1)
    cv2.circle(img, (180, 80), 8, 0, -1)
    cv2.circle(img, (220, 120), 8, 0, -1)

    return img

# ===================== 闭运算 =====================

holed = create_holed_image()
kernel = cv2.getStructuringElement(cv2.MORPH_RECT, (15, 15))

# 方法1：使用 morphologyEx
closed = cv2.morphologyEx(holed, cv2.MORPH_CLOSE, kernel)

# 方法2：等价于先膨胀后腐蚀
dilated = cv2.dilate(holed, kernel)
closed_manual = cv2.erode(dilated, kernel)

# ===================== 可视化 =====================

fig, axes = plt.subplots(2, 2, figsize=(12, 10))

axes[0, 0].imshow(holed, cmap='gray')
axes[0, 0].set_title('原图（带空洞）', fontsize=12)
axes[0, 0].axis('off')

axes[0, 1].imshow(closed, cmap='gray')
axes[0, 1].set_title('闭运算结果', fontsize=12)
axes[0, 1].axis('off')

axes[1, 0].imshow(dilated, cmap='gray')
axes[1, 0].set_title('中间步骤：膨胀', fontsize=12)
axes[1, 0].axis('off')

axes[1, 1].imshow(closed_manual, cmap='gray')
axes[1, 1].set_title('膨胀后腐蚀（= 闭运算）', fontsize=12)
axes[1, 1].axis('off')

plt.suptitle('闭运算填洞效果', fontsize=14, fontweight='bold')
plt.tight_layout()
plt.savefig('closing_basic.png', dpi=150)
plt.show()

print("两种方法结果是否一致:", np.array_equal(closed, closed_manual))
```

---

### 代码3：开闭运算组合使用

```python
"""
开运算和闭运算组合使用
同时去除噪点和填补空洞
"""

import cv2
import numpy as np
import matplotlib.pyplot as plt

plt.rcParams['font.sans-serif'] = ['SimHei', 'Microsoft YaHei']
plt.rcParams['axes.unicode_minus'] = False

# ===================== 创建复杂测试图像 =====================

def create_complex_image():
    """创建同时有噪点和空洞的图像"""
    img = np.zeros((200, 300), dtype=np.uint8)

    # 主要物体（带空洞）
    cv2.rectangle(img, (40, 40), (140, 160), 255, -1)
    cv2.circle(img, (90, 100), 20, 0, -1)  # 内部空洞

    cv2.circle(img, (210, 100), 50, 255, -1)
    cv2.circle(img, (210, 100), 15, 0, -1)  # 内部空洞

    # 添加噪点
    np.random.seed(123)
    for _ in range(50):
        x = np.random.randint(0, 300)
        y = np.random.randint(0, 200)
        cv2.circle(img, (x, y), np.random.randint(2, 5), 255, -1)

    return img

# ===================== 开闭运算组合 =====================

original = create_complex_image()
kernel = cv2.getStructuringElement(cv2.MORPH_ELLIPSE, (11, 11))

# 策略1：先开后闭
opened = cv2.morphologyEx(original, cv2.MORPH_OPEN, kernel)
open_then_close = cv2.morphologyEx(opened, cv2.MORPH_CLOSE, kernel)

# 策略2：先闭后开
closed = cv2.morphologyEx(original, cv2.MORPH_CLOSE, kernel)
close_then_open = cv2.morphologyEx(closed, cv2.MORPH_OPEN, kernel)

# ===================== 可视化 =====================

fig, axes = plt.subplots(2, 3, figsize=(15, 9))

# 先开后闭
axes[0, 0].imshow(original, cmap='gray')
axes[0, 0].set_title('原图', fontsize=12)
axes[0, 0].axis('off')

axes[0, 1].imshow(opened, cmap='gray')
axes[0, 1].set_title('① 开运算（去噪）', fontsize=12)
axes[0, 1].axis('off')

axes[0, 2].imshow(open_then_close, cmap='gray')
axes[0, 2].set_title('② 闭运算（填洞）', fontsize=12)
axes[0, 2].axis('off')

# 先闭后开
axes[1, 0].imshow(original, cmap='gray')
axes[1, 0].set_title('原图', fontsize=12)
axes[1, 0].axis('off')

axes[1, 1].imshow(closed, cmap='gray')
axes[1, 1].set_title('① 闭运算（填洞）', fontsize=12)
axes[1, 1].axis('off')

axes[1, 2].imshow(close_then_open, cmap='gray')
axes[1, 2].set_title('② 开运算（去噪）', fontsize=12)
axes[1, 2].axis('off')

# 添加行标签
fig.text(0.02, 0.72, '先开后闭', fontsize=12, fontweight='bold', rotation=90, va='center')
fig.text(0.02, 0.28, '先闭后开', fontsize=12, fontweight='bold', rotation=90, va='center')

plt.suptitle('开闭运算组合对比', fontsize=14, fontweight='bold')
plt.tight_layout()
plt.subplots_adjust(left=0.06)
plt.savefig('open_close_combination.png', dpi=150)
plt.show()

print("对比结论：")
print("• 先开后闭：先去噪再填洞，边缘更平滑")
print("• 先闭后开：先填洞再去噪，可能保留一些噪点")
print("• 两种结果略有差异，一般推荐先开后闭")
```

---

### 代码4：结构元素大小的影响

```python
"""
演示结构元素大小对开闭运算效果的影响
"""

import cv2
import numpy as np
import matplotlib.pyplot as plt

plt.rcParams['font.sans-serif'] = ['SimHei', 'Microsoft YaHei']
plt.rcParams['axes.unicode_minus'] = False

# 创建测试图像
def create_test():
    img = np.zeros((150, 150), dtype=np.uint8)
    cv2.circle(img, (75, 75), 50, 255, -1)
    # 添加不同大小的噪点
    cv2.circle(img, (30, 30), 5, 255, -1)   # 小噪点
    cv2.circle(img, (120, 30), 10, 255, -1)  # 中噪点
    cv2.circle(img, (30, 120), 15, 255, -1)  # 大噪点
    return img

original = create_test()

# 不同大小的核
kernel_sizes = [5, 11, 21, 31]

fig, axes = plt.subplots(1, len(kernel_sizes) + 1, figsize=(15, 3))

axes[0].imshow(original, cmap='gray')
axes[0].set_title('原图', fontsize=11)
axes[0].axis('off')

for i, size in enumerate(kernel_sizes):
    kernel = cv2.getStructuringElement(cv2.MORPH_ELLIPSE, (size, size))
    opened = cv2.morphologyEx(original, cv2.MORPH_OPEN, kernel)

    axes[i + 1].imshow(opened, cmap='gray')
    axes[i + 1].set_title(f'开运算 {size}×{size}', fontsize=11)
    axes[i + 1].axis('off')

plt.suptitle('结构元素大小对开运算的影响', fontsize=14, fontweight='bold')
plt.tight_layout()
plt.savefig('kernel_size_opening.png', dpi=150)
plt.show()

print("观察结论：")
print("• 小核（5×5）：只能去除最小的噪点")
print("• 中核（11×11）：可以去除小、中噪点")
print("• 大核（21×21+）：可以去除更大的噪点，但可能影响主物体")
print("\n选择原则：核大小应略大于噪点，略小于最小的有效物体")
```

---

### 代码5：实际应用 - 文档图像处理

```python
"""
实际应用：使用开闭运算处理文档扫描图像
去除噪点、填补断裂的文字
"""

import cv2
import numpy as np
import matplotlib.pyplot as plt

plt.rcParams['font.sans-serif'] = ['SimHei', 'Microsoft YaHei']
plt.rcParams['axes.unicode_minus'] = False

# ===================== 模拟文档图像 =====================

def create_document_image():
    """创建模拟的文档扫描图像（带噪点和断裂）"""
    img = np.zeros((200, 400), dtype=np.uint8)

    # 模拟文字（几个矩形条）
    cv2.rectangle(img, (30, 40), (180, 60), 255, -1)
    cv2.rectangle(img, (30, 80), (220, 100), 255, -1)
    cv2.rectangle(img, (30, 120), (190, 140), 255, -1)

    # 添加一些断裂
    cv2.rectangle(img, (80, 40), (90, 60), 0, -1)
    cv2.rectangle(img, (140, 80), (150, 100), 0, -1)
    cv2.rectangle(img, (100, 120), (108, 140), 0, -1)

    # 添加噪点（模拟扫描噪声）
    np.random.seed(42)
    noise = np.random.random((200, 400))
    img[noise < 0.02] = 255  # 随机白点
    img[(noise > 0.98) & (img == 255)] = 0  # 随机黑点在文字上

    # 模拟扫描边缘噪声
    for _ in range(30):
        x = np.random.randint(250, 380)
        y = np.random.randint(20, 180)
        cv2.circle(img, (x, y), np.random.randint(1, 4), 255, -1)

    return img

# ===================== 处理流程 =====================

document = create_document_image()

# 定义结构元素
kernel_open = cv2.getStructuringElement(cv2.MORPH_RECT, (3, 3))
kernel_close = cv2.getStructuringElement(cv2.MORPH_RECT, (5, 5))

# 步骤1：开运算去噪
step1 = cv2.morphologyEx(document, cv2.MORPH_OPEN, kernel_open)

# 步骤2：闭运算填补断裂
step2 = cv2.morphologyEx(step1, cv2.MORPH_CLOSE, kernel_close)

# ===================== 可视化 =====================

fig, axes = plt.subplots(1, 3, figsize=(15, 4))

axes[0].imshow(document, cmap='gray')
axes[0].set_title('原始扫描图像\n（有噪点和断裂）', fontsize=11)
axes[0].axis('off')

axes[1].imshow(step1, cmap='gray')
axes[1].set_title('开运算后\n（噪点去除）', fontsize=11)
axes[1].axis('off')

axes[2].imshow(step2, cmap='gray')
axes[2].set_title('闭运算后\n（断裂修复）', fontsize=11)
axes[2].axis('off')

plt.suptitle('文档图像处理实例', fontsize=14, fontweight='bold')
plt.tight_layout()
plt.savefig('document_processing.png', dpi=150)
plt.show()

print("处理流程：")
print("1. 开运算（3×3）：去除背景噪点")
print("2. 闭运算（5×5）：填补文字断裂")
print("\n这是 OCR 预处理的常用步骤！")
```

---

### 代码6：实际应用 - 指纹图像处理

```python
"""
实际应用：指纹图像增强
使用形态学操作改善指纹图像质量
"""

import cv2
import numpy as np
import matplotlib.pyplot as plt

plt.rcParams['font.sans-serif'] = ['SimHei', 'Microsoft YaHei']
plt.rcParams['axes.unicode_minus'] = False

# ===================== 模拟指纹图像 =====================

def create_fingerprint_image():
    """创建模拟的指纹图像"""
    img = np.zeros((200, 200), dtype=np.uint8)

    # 绘制指纹纹路（一系列曲线）
    for i in range(10):
        y_base = 20 + i * 18
        pts = []
        for x in range(10, 190):
            y = y_base + int(10 * np.sin(x / 15 + i * 0.5))
            pts.append([x, y])
        pts = np.array(pts, dtype=np.int32)
        cv2.polylines(img, [pts], False, 255, 2)

    # 添加断裂
    for _ in range(20):
        x = np.random.randint(20, 180)
        y = np.random.randint(20, 180)
        cv2.circle(img, (x, y), np.random.randint(3, 8), 0, -1)

    # 添加噪点
    noise = np.random.random((200, 200))
    img[noise < 0.03] = 255
    img[noise > 0.97] = 0

    return img

# ===================== 处理流程 =====================

fingerprint = create_fingerprint_image()

# 方案1：先开后闭
kernel = cv2.getStructuringElement(cv2.MORPH_ELLIPSE, (3, 3))
opened = cv2.morphologyEx(fingerprint, cv2.MORPH_OPEN, kernel)
method1 = cv2.morphologyEx(opened, cv2.MORPH_CLOSE, kernel)

# 方案2：使用横向核连接纹路
kernel_h = cv2.getStructuringElement(cv2.MORPH_RECT, (5, 1))
method2 = cv2.morphologyEx(fingerprint, cv2.MORPH_CLOSE, kernel_h)
method2 = cv2.morphologyEx(method2, cv2.MORPH_OPEN, kernel)

# ===================== 可视化 =====================

fig, axes = plt.subplots(1, 3, figsize=(12, 4))

axes[0].imshow(fingerprint, cmap='gray')
axes[0].set_title('原始指纹图像', fontsize=11)
axes[0].axis('off')

axes[1].imshow(method1, cmap='gray')
axes[1].set_title('方案1：先开后闭\n（3×3 椭圆核）', fontsize=11)
axes[1].axis('off')

axes[2].imshow(method2, cmap='gray')
axes[2].set_title('方案2：横向闭+开\n（5×1 矩形核）', fontsize=11)
axes[2].axis('off')

plt.suptitle('指纹图像增强', fontsize=14, fontweight='bold')
plt.tight_layout()
plt.savefig('fingerprint_enhancement.png', dpi=150)
plt.show()

print("技巧提示：")
print("• 指纹是横向纹路，使用横向核（5×1）更有效")
print("• 先闭后开可以先连接断裂的纹路")
print("• 核的方向应该与纹路方向一致")
```

---

## 📝 本节总结

```
┌────────────────────────────────────────────────────────────────┐
│                    开运算与闭运算总结                           │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 开运算 (Opening)                                           │
│     ┌──────────────────────────────────────────────────────┐   │
│     │ 定义：先腐蚀后膨胀                                   │   │
│     │ 函数：cv2.morphologyEx(img, cv2.MORPH_OPEN, kernel)  │   │
│     │ 效果：去除白色噪点、平滑边缘突起、断开细连接         │   │
│     │ 记忆：像开门赶噪点出去                               │   │
│     └──────────────────────────────────────────────────────┘   │
│                                                                │
│  2. 闭运算 (Closing)                                           │
│     ┌──────────────────────────────────────────────────────┐   │
│     │ 定义：先膨胀后腐蚀                                   │   │
│     │ 函数：cv2.morphologyEx(img, cv2.MORPH_CLOSE, kernel) │   │
│     │ 效果：填补黑色空洞、填平边缘凹陷、连接邻近物体       │   │
│     │ 记忆：像关门把洞填上                                 │   │
│     └──────────────────────────────────────────────────────┘   │
│                                                                │
│  3. 组合使用                                                    │
│     • 先开后闭：推荐，先去噪再填洞                             │
│     • 先闭后开：先填洞再去噪                                   │
│                                                                │
│  4. 核大小选择                                                  │
│     • 核大小 > 噪点/空洞大小                                   │
│     • 核大小 < 最小有效物体/间隙                               │
│                                                                │
│  5. 常见应用                                                    │
│     • 文档处理：去噪点、修复断字                               │
│     • 指纹识别：纹路增强、断裂修复                             │
│     • 医学图像：细胞分析预处理                                 │
│     • 工业检测：缺陷检测预处理                                 │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 🎯 下一步学习

在下一节 **"04-梯度运算"** 中，我们将：
- 学习使用形态学梯度提取物体边缘
- 了解内梯度和外梯度
- 掌握边缘提取的实际应用

---

> 💡 **实践建议**：尝试在自己的项目中使用开闭运算！特别是在进行 OCR 或目标检测之前，形态学预处理能大大提高准确率。
